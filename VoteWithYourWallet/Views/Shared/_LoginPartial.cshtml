@using Microsoft.AspNet.Identity
@using Microsoft.AspNet.Identity.EntityFramework
@using VoteWithYourWallet.Models
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        var userID = User.Identity.GetUserId();
        var manager = new UserManager<ApplicationUser>(new UserStore<ApplicationUser>(new ApplicationDbContext()));
        var currentUser = manager.FindById(userID);
        var FirstName = currentUser.FirstName;

        <ul class="navbar-nav navbar-right">
            <li class="nav-item">
                @Html.ActionLink("Hello " + FirstName + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class="nav-link" })
            </li>
            <li class="nav-item"><a class="btn btn-danger" href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    }
}
else
{
    <div class="btn-group navbar-btn">
        <button type="button" class="btn btn-success" id="signUpButtonMain" data-toggle="modal" data-target="#loginModal" data-whatever="signUp" aria-controls="signUp">Sign up</button>
        <button type="button" class="btn btn-success" id="signInButtonMain" data-toggle="modal" data-target="#loginModal" data-whatever="signIn" aria-controls="signIn">Log in</button>
    </div>
    @*<ul class="nav navbar-nav navbar-right">
            <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        </ul>*@
}
        </div>
<!-- /.navbar-collapse -->
    </nav>
<!-- Login Modal-->
<div class="modal fade" id="loginModal" tabindex="-1" role="dialog" aria-labelledby="loginModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="loginModalLabel">Sign Up or Log In</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <nav>
                    <div class="nav nav-tabs" id="signUpOrInTabs" role="tablist">
                        <a class="nav-item nav-link active" href="#signUp" id="signUp-tab" data-toggle="tab" role="tab" aria-controls="signUp" aria-selected="true">Sign up</a>
                        <a class="nav-item nav-link" href="#signIn" id="signIn-tab" data-toggle="tab" role="tab" aria-controls="signIn" aria-selected="false">Log in</a>
                    </div>
                </nav>
                <div class="tab-content" id="logintabContent">
                    <!-- Sign Up tab -->
                    <div class="tab-pane fade show active" id="signUp" role="tabpanel" aria-labelledby="signUp-tab">
                        @Html.Partial("_RegisterPartial", new RegisterViewModel())
                    </div>
                    <!-- Sign In tab -->
                    <div class="tab-pane fade show" id="signIn" role="tabpanel" aria-labelledby="signIn-tab">
                        @Html.Partial("_LoginModalPartial", new LoginViewModel())
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- /. Login Modal -->
